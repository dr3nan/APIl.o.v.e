import express from 'express';

const router = express.Router();

// we define the endpoint for the GET request

// we define the endpoint for the POST request

// we define the endpoint for the PUT request

// we define the endpoint for the DELETE request

export default router;
